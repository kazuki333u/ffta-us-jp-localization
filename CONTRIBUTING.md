# 参加のしかた

**バグ報告はいちばんありがたい貢献です。**
GitHub に慣れていなくても構いません。まずは気軽に報告してください。

---

## いちばん簡単な参加方法

[Issue を作成する](../../issues/new/choose) — これだけです。

「日本語がおかしい」「文字がはみ出している」「ここだけ英語のまま」
といった報告が、公開ベータでいちばん役に立ちます。

**先に [`docs/KNOWN_ISSUES.md`](docs/KNOWN_ISSUES.md) を見ていただけると
助かりますが、重複していても構いません。** 報告されない問題より、
2回報告される問題の方がずっとましです。

---

## 大切なお願い — 添付物について

### ROMファイルは絶対に添付しないでください

Issue、PR、コメント、いずれにも **`.gba` ファイルを添付しないでください。**
リンクも貼らないでください。

必要なのはROMそのものではなく、**ハッシュ**です。

```
python build.py --identify FFTA_US_JP.gba
```

の出力を貼ってください。

### セーブデータの添付は任意です

再現に役立つ場合がありますが、**必須ではありません**。

添付する場合は次にご注意ください。

- セーブデータには**あなたが入力したキャラクター名・クラン名**が
  含まれます。
- 個人が特定される情報 (本名など) を入れていないか確認してください。
- ファイル名にも注意してください。

### `.build/` を添付しないでください

`--keep-work` で残る `.build/` には**あなたのROMのコピー**が入っています。
Issue へ添付しないでください。

添付するのは `.build/build.log` の**末尾**だけで十分です。

---

## 歓迎する貢献

| 種類 | 説明 | 必要な技術 |
|---|---|---|
| **誤字・誤訳の報告** | 日本語がおかしい箇所 | なし |
| **表示の報告** | はみ出し・重なり・文字化け | なし |
| **クラッシュの報告** | 固まる・落ちる・画面が壊れる | なし |
| **終盤のテスト** | 終盤・US版追加コンテンツの確認 | なし (時間はかかります) |
| **エミュレータ / 実機の差異** | 特定環境でのみ起きる問題 | なし |
| **再現手順の追加** | 既存Issueに再現方法を足す | なし |
| **ドキュメントの改善** | 分かりにくい説明の修正 | 少し |
| **テストの追加** | ROM不要の検査を増やす | Python |
| **コード修正** | 範囲の明確な修正 | Python + ROM構造の理解 |

**終盤のテストは特に不足しています。** 本編を最後までプレイして
「特に問題なかった」という報告でも、十分に価値があります。

---

## 誤字の修正だけしたい場合

翻訳文は `src/localizer/chain/data/` の JSON にあります。
`japanese` フィールドを直接編集する PR を送っていただけます。

ただし次にご注意ください。

- **文字幅の制約があります。** 表示枠に収まらない長さにすると
  ビルドが停止します (`*_WIDTH_*` 系のエラー)。
- **使える漢字に制約があります。** 日本版ROMに存在するグリフしか
  使えません。存在しない文字を書くとビルドが停止します。
- 変更後は**必ずビルドが通ることを確認**してください。
  出力ROMのハッシュは変わります (それが正しい状態です)。

自信がない場合は、**PRではなくIssueで「ここをこう直したい」と
提案するだけでも構いません。** こちらで対応します。

---

## コードを変更する場合

### 求めるもの

ROMのオフセットを直接書き換えただけの変更は受け付けられません。
本プロジェクトは**決定的なビルド**が成立していることが前提です。

PR には次を含めてください。

1. **ビルドチェーンへの変更として実装されていること**
   (`src/localizer/chain/` のモジュール、またはマニフェスト)
2. **ビルドが通ること** — 変更後の出力ROMのSHA-256を書いてください
3. **再現手順** — 何が問題で、どう直ったのか
4. **リグレッション確認** — 直そうとした箇所以外が壊れていないこと

### 出力ハッシュが変わることについて

ビルドの出力を変える修正では、`build.py` の `EXPECTED_OUTPUT` が
一致しなくなります。**それは正常です。**

PR では `EXPECTED_OUTPUT` を書き換えず、
**新しいハッシュを PR の説明に書いてください。**
リリース時にまとめて更新します。

### 絶対にやらないこと

- ROM、セーブ、セーブステート、パッチ、アーカイブをコミットする
- ゲームの公式テキスト・フォント・グラフィックをリポジトリへ追加する
  (英語原文をマニフェストへ書き戻すことを含みます —
  [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) §3 を参照)
- ビルド生成物 (`.build/`、出力ROM、ログ) をコミットする
- ローカルの絶対パスや個人情報を含める

これらは `tools/public_audit.py` が機械的に検出し、CI が落ちます。
安全性・プライバシー上の取り扱いは [`SECURITY.md`](SECURITY.md) を
参照してください。

---

## ローカルでの確認方法

### ROM不要のチェック (CIと同じもの)

```
python -m pip install -r requirements-dev.txt
python tools/public_audit.py
python -m pytest tests/public -q
```

### ROMが必要な確認

```
python build.py --us FFTA_US.gba --jp FFTA_JP.gba --output out.gba
python tools/public_audit.py --us FFTA_US.gba --jp FFTA_JP.gba
```

詳細は [`docs/LOCAL_ROM_QA.md`](docs/LOCAL_ROM_QA.md) を参照してください。

---

## ライセンスについて

本リポジトリのコードは **GPL-3.0** です
([`LICENSE`](LICENSE)、[`NOTICE.md`](NOTICE.md))。

貢献していただいた内容も同じライセンスで公開されます。

**他所からコードや翻訳文を持ち込む場合は、出所とライセンスを
明記してください。** 出所が不明なものは受け付けられません。

---

## 行動について

- 相手を尊重してください。
- **ROMの入手方法を尋ねたり、共有を申し出たりしないでください。**
  そうしたIssue・コメントは削除されます。
- 分からないことは遠慮なく聞いてください。
  「初歩的すぎる質問」というものはありません。
