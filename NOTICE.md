# NOTICE — 第三者著作物と帰属表示

このリポジトリは複数の出所のコードを含みます。
どこまでが本プロジェクトの著作物で、どこからが第三者の著作物かを以下に明記します。

License summary: **GNU General Public License v3.0** (`LICENSE`).

---

## 1. 上流プロジェクト (third-party, GPL-3.0)

本プロジェクトのROM解析・テキストパーサ層は、以下の第三者プロジェクトから派生しています。

| 項目 | 値 |
|---|---|
| 上流プロジェクト | `ffta_us_cn` |
| URL | https://github.com/BSoD123456/ffta_us_cn |
| 取り込んだ上流コミット | `cd32ebce88bb26cae030115d5538c1d01a0772f3` (2024-01-04) |
| 上流のライセンス | GNU General Public License v3.0 (上流リポジトリ同梱の `LICENSE`) |
| 本リポジトリの `LICENSE` | 上流の `LICENSE` と**バイト同一** (SHA-256 `3972DC9744F6499F0F9B2DBF76696F2AE7AD8AF9B23DDE66D6AF86C9DFB36986`、35,149 バイト、GPL-3.0 原文) |

上流プロジェクトは、FFTA の US 版を**中国語**へローカライズするためのツール群です。
本プロジェクトはその ROM 構造解析・テキストセクションパーサ・文字セット処理を再利用し、
**日本語**ローカライズのために別方向へ発展させたものです。

### 本リポジトリに含まれる上流由来ファイル

`src/localizer/chain/` 内の以下 8 モジュールは上流由来です。

| ファイル | 役割 |
|---|---|
| `ffta_sect.py` | ROM セクション / テーブル構造の読み出し |
| `ffta_parser.py` | テキストレコードおよびスクリプトのパーサ |
| `ffta_charset.py` | 文字セットの解決 |
| `ffta_modifier.py` | ROM 改変フレームワーク |
| `ffta_font.py` | フォントビットマップの描画 |
| `ffta_font_generator.py` | フォント生成 |
| `ffta_ocr.py` | OCR 補助 (本ビルドでは未使用。遅延 import のみ) |
| `ffta_ocr_ambi.py` | OCR 曖昧性解決 (同上) |

同じく上流由来のデータファイル:

| ファイル | 内容 |
|---|---|
| `charset_us.json` | US 版 ROM の文字セットテーブル |
| `charset_cn.json` | 上流の中国語文字セットテーブル (パーサが参照) |

機械可読な内訳は `src/localizer/chain/SYNC_MANIFEST.json` の
`upstream_modules` を参照してください。

### 上流からの変更点 (GPL-3.0 §5(a) の表示)

上記 8 モジュールのうち、本プロジェクトが変更しているのは
**`ffta_modifier.py` の 1 ファイルのみ**です。
残る 7 モジュールは、上記の上流コミットの内容と
**改行コードを LF に正規化した上でバイト同一**です。

`ffta_modifier.py` の変更は、レコード対応表 (`align` 設定) の追加・修正に
限られます。US 版が日本版に対して台詞を挿入している箇所で、
生インデックスによる対応付けが**ずれたスロットへ書き込む**のを防ぐための
アンカー指定です。実行ロジックの変更は含みません。

### 上流プロジェクトのクレジット (上流 `credits.txt` より)

```
项目主页: https://github.com/BSoD123456/ffta_us_cn

原汉化版人员名单:
  熊组
  程序: 暗黑破坏熊
  翻译: AYAHR CHDU 台湾玩人

修正版人员名单:
  修正: yggdra

美版汉化人员名单:
  程序: Tring
  翻译: liekong CnIV
```

### 本リポジトリに**含めなかった**上流ファイル

以下は上流に存在しますが、本プロジェクトのビルドに不要なため取り込んでいません。
GPL は著作物全体の再配布を強制しないため、これは許容されます。

| ファイル | 除外理由 |
|---|---|
| `trans_txt.json` (227 KB) | 上流の**中国語翻訳テキスト本体**。本プロジェクトと無関係 |
| `trans_fix_txt.json` | 同上 |
| `trans_checker.py` / `trans_formatter.py` | 上記翻訳データ専用のツール |
| `tips_and_test.py` | 上流の実験用スクリプト |
| `ffta_finder.py` | 本ビルドチェーンから到達しない探索ツール |
| `font/BoutiqueBitmap9x9_1.7.ttf` | 第三者ビットマップフォント。**下記 §2 参照** |
| `readme.md` / `credits.txt` | 上流の説明文書 (帰属内容は本ファイルへ転記済み) |

---

## 2. 除外した第三者フォント — Boutique Bitmap 9x9

上流リポジトリには `font/BoutiqueBitmap9x9_1.7.ttf` が同梱されています。

**本リポジトリはこのフォントを同梱しません。**

理由:

- 上流の同梱物であり、上流の `LICENSE` (GPL-3.0) は上流の*プログラム*に対する
  宣言です。同梱フォントが同じ条件で再配布可能かは、上流リポジトリ内に
  フォント固有のライセンス表記がないため**確認できませんでした**。
- 本プロジェクトのビルドはこのフォントを必要としません。
  日本語グリフはすべて**ユーザー自身の JP 版 ROM** から取得します
  (`docs/ARCHITECTURE.md`)。
- 参照している `ffta_modifier.py` のコードパスは、本ビルドチェーンでは
  実行されません。

不要なものの provenance を確定させるより、除外する方が安全と判断しました。
将来このフォントが必要になった場合は、再配布条件を確認するまで同梱しません。

---

## 3. 本プロジェクトの著作物 (project-authored)

以下は本プロジェクトが新規に作成したものであり、GPL-3.0 で提供します。

- `build.py`、`tools/`、`tests/`
- `src/localizer/chain/` のうち上流由来 8 モジュールを除く 44 モジュール
  (`ffta_jp_*.py`、`ffta_us_source.py`、`ffta_placeholder_transfer_audit.py`、
  `ffta_qa_glyphs.py`、`ffta_name_entry_kbd.py`、`ffta_gfx_codec.py`)
- `src/localizer/chain/data/` 内マニフェストの **日本語訳文**、および
  レコード ID・座標・幅・語彙方針などのメタデータ
- `docs/`、`README.md`、`CONTRIBUTING.md`、`.github/`

---

## 4. ゲーム本体の著作物 — 本リポジトリのライセンス対象外

Final Fantasy Tactics Advance およびその ROM、テキスト、フォント、
グラフィック、音声等の一切は **株式会社スクウェア・エニックス** の著作物です。

- 本リポジトリは **ROM を一切含みません**。
- 本リポジトリは公式テキスト・フォント・グラフィックを**同梱しません**。
  すべてビルド時にユーザー自身の ROM から取得されます
  (`docs/ARCHITECTURE.md`、`docs/PROVENANCE.md`)。
- `LICENSE` (GPL-3.0) は本リポジトリの**プログラムとプロジェクト著作物**に
  対してのみ適用され、ゲーム本体の著作物には及びません。
- 本プロジェクトはスクウェア・エニックスおよびその関連会社と
  **一切関係がありません**。非公式のファンプロジェクトです。

---

## 5. ライセンスに関する注意

本ファイルは出所調査の記録であり、法的助言ではありません。
上流が GPL-3.0 を明示しているため本リポジトリも GPL-3.0 を採用していますが、
懸念がある場合は各自でご確認ください。
