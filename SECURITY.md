# セキュリティとプライバシーについて

このリポジトリは、ユーザーのローカル環境でROMを加工するツールです。
安全性・プライバシー上、ユーザーとメンテナが知っておくべき事項をまとめます。

---

## 1. このツールが行うこと / 行わないこと

**行うこと**

- コマンドラインで指定された2本のROMファイルを**読み取る**
- 作業ディレクトリ (既定 `./.build`) に一時ファイルを**書き出す**
- 指定された出力パスに1本のROMを**書き出す**

**行わないこと**

- ネットワーク通信 (**一切行いません**)
- ROM・ログ・統計情報の外部送信
- 環境変数・認証情報・個人情報の読み取り
- 指定されたパス以外への書き込み
- 常駐・自動起動・スケジュール登録

ビルドは完全にオフラインで完結します。

---

## 2. 作業ディレクトリ (`.build/`) の取り扱い

`.build/` には**あなたのROMのコピー**が置かれます。

- ビルドが成功すると**自動的に削除**されます。
- `--keep-work` を指定した場合のみ残ります。
- `.gitignore` により Git の追跡対象から除外されています。

**このフォルダを Issue に添付したり、共有ストレージへ置いたりしないでください。**

---

## 3. 不具合報告時に添付してはいけないもの

| 種別 | 理由 |
|---|---|
| **ROMファイル** (`.gba` など) | 著作物そのものです |
| **BPS / IPS / UPS パッチ** | 実質的にROMデータを含みます |
| **`.build/` フォルダ** | ROMのコピーが入っています |
| **ROMから抽出した画像・テキストダンプ** | 著作物の一部です |

**添付してよいもの**

- `build.py` が表示したエラーメッセージ
- `.build/build.log` の**末尾**
- 出力ROMの SHA-256 (ハッシュ値そのものはデータではありません)
- ゲーム画面のスクリーンショット (不具合の説明に必要な範囲で)

### セーブデータについて

セーブデータ (`.sav`) の添付は**任意**です。添付する場合、
プレイヤーが入力した**キャラクター名・クラン名**が含まれる点に
ご注意ください。実名やハンドルネームを入れている場合は特にご注意ください。

---

## 4. リポジトリに含めてはいけないもの

本リポジトリは**テキストのみ**で構成されています。
次のものは、コミットにも Pull Request にも含められません。

- ROM (US版 / JP版 / 生成物のいずれも)
- BPS / IPS / UPS などの差分パッチ
- セーブデータ・セーブステート
- ROMから抽出したグラフィック・フォント・テキストダンプ
- 日本版・US版の原文コーパス
- 開発マシンの絶対パス・ユーザー名・メールアドレス
- アクセストークン・APIキー・秘密鍵・認証情報

これらは `tools/public_audit.py` が機械的に検査します。

```
python tools/public_audit.py
```

さらに、ROMを手元に持っている場合は
**ROMのバイト列がリポジトリ内へ紛れ込んでいないか**も検査できます。

```
python tools/public_audit.py --us <us.gba> --jp <jp.gba>
```

この検査は、両ROMのすべての32バイト窓を集合化し、
追跡対象の全ファイルと突き合わせます。
1件でも一致すれば失敗として報告されます。

---

## 5. CI について

CI (GitHub Actions) は**ROMを一切必要としません**。

- ROMをリポジトリシークレットとして要求する設計にはしません。
- ROMやビルド生成物を CI アーティファクトとして保存しません。
- ROMをダウンロードする処理を含めません。

CI が行うのは、公開監査・構文検査・ROM不要のテストだけです。
ROMが必要な検証は [`docs/LOCAL_ROM_QA.md`](docs/LOCAL_ROM_QA.md) の通り
ローカル専用です。

---

## 6. 脆弱性・混入の報告

次に該当するものを見つけた場合は、**公開 Issue を立てる前に**
メンテナへ非公開で連絡してください。

- リポジトリ内にROM由来のデータが含まれている
- 認証情報・秘密鍵・個人情報が含まれている
- ビルドスクリプトが意図しないファイルを読み書きする

GitHub の **Security → Report a vulnerability** (Private vulnerability
reporting) を利用してください。

それ以外の一般的な不具合は、通常どおり Issue で報告してください。

---

## 7. 免責

本プロジェクトはボランティアによる非公式のファンプロジェクトです。
プログラムは GPL-3.0 の定めるとおり**無保証**で提供されます
([`LICENSE`](LICENSE))。

利用が適法であることについても保証しません。
利用にあたっては、お住まいの地域の法令等をご自身でご確認ください。
